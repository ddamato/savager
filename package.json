{
  "name": "savager",
  "version": "0.0.1",
  "description": "SVG Manager Solution",
  "main": "./dist/savager.min.js",
  "browser": "./dist/savager.iife.js",
  "scripts": {
    "bundle": "rollup -c",
    "build:examples": "npm run bundle && mkdir -p ./examples/savager && cp -r ./dist/*.js ./examples/savager",
    "test": "echo \"Error: no test specified\" && exit 1",
    "version": "auto-changelog -p && git add CHANGELOG.md"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ddamato/savager.git"
  },
  "keywords": [],
  "author": "Donnie D'Amato <fauxserious@gmail.com>",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/ddamato/savager/issues"
  },
  "homepage": "https://github.com/ddamato/savager#readme",
  "devDependencies": {
    "auto-changelog": "^2.0.0",
    "rollup": "^2.13.1",
    "rollup-plugin-terser": "^6.1.0"
  }
}
